import React from "react";

// this decoder will turn Uint8Arrays into strings

const download = async (fs, text) => {};

export default function AccessHome() {
  console.log("Added file contents:");
  return <div>hello</div>;
}
